# 3D/Convert-2d-to-3d-example01.py — 단안 깊이(DPT), PLY/OBJ, Gradio UI
#
# 디바이스 (스크립트 동작)
#   - NVIDIA CUDA GPU가 보이면 CUDA 사용
#   - CUDA 없고 Apple Silicon MPS만 가능하면 MPS 사용
#   - 위 가속기가 없으면 CPU만 사용
#
# PyTorch 설치
#   - 아래 `torch` 한 줄은 PyPI 기본(CPU) 휠일 수 있습니다.
#   - NVIDIA GPU를 쓰려면 설치 후에도 반드시 확인하세요:
#       python -c "import torch; print(torch.cuda.is_available())"
#     True가 아니면 CPU 빌드이거나 드라이버/CUDA 불일치입니다.
#   - CUDA 빌드는 PyTorch 공식 선택기로 설치하는 것을 권장합니다:
#       https://pytorch.org/get-started/locally/
#     (예: pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cu124)
#   - 그 다음 이 파일의 나머지 패키지를 설치하면 됩니다.

torch>=2.1.0
transformers>=4.36.0
numpy>=1.24.0
Pillow>=10.0.0
gradio>=4.19.0
